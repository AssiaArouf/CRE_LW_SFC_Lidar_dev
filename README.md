# CRE_LW_SFC_Lidar_dev
This repo contains codes used to derive the surface LW cloud radiative effect (CRE) from CALIPSO space lidar observations and radiative transfer computation. 
The coefficients 'a' and 'b' derived from the linear regressions are also available in a .nc file.
